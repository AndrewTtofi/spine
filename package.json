{
  "name": "spine",
  "version": "1.3.0",
  "private": true,
  "description": "Root scripts for the spine plugin: the validate harness and its tests (the dashboard/ package has its own).",
  "license": "MIT",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test scripts/"
  }
}
